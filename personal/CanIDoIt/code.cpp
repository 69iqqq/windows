/* __ ___       _____ ____   ____   ____
  / // _ \     |_   _/ __ \ / __ \ / __ \
 / /| (_) |______| || |  | | |  | | |  | |
| '_ \__, |______| || |  | | |  | | |  | |
| (_) |/ /      _| || |__| | |__| | |__| |
 \___//_/      |_____\___\_\\___\_\\___\_\
*/

/*Do Development there is nothing in doing cp, it's a waste of time; lol
  sobbing cp is too hard for me to handle*/
#include <algorithm>
#include <cmath>
#include <cstdint>
#include <iostream>
#include <limits.h>
#include <map>
#include <string>
#include <unordered_map>
#include <vector>
using std::ios, std::ios_base, std::cin, std::cout, std::vector, std::min,
    std::max, std::abs, std::string, std::unordered_map, std::map, std::pair,
    std::endl;
#define int long long int
#define vi vector<int>
#define vl vector<long long>
#define pb push_back
#define f first
#define s second
#define all(a) a.begin(), a.end()
#define gcd(a, b) __gcd(a, b)
#define lcm(a, b) (a * b) / gcd(a, b)
#define yes(check) (check) ? cout << "YES" << '\n' : cout << "NO" << '\n'
#define forn(i, j, k) for (int i = j; i < k; i++)
#define big INT_MAX
#define smol INT_MIN
#define e "\n"
const int MOD = 1e9 + 7;

void solve() {}

int32_t main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  int T = 1;
  cin >> T;
  while (T--) {
    solve();
  }
  return 0;
}
